import { StoryReactionOverlay } from '@/components/feed/StoryReactionOverlay';

describe('StoryReactionOverlay', () => {
  it('exports StoryReactionOverlay component correctly', () => {
    expect(StoryReactionOverlay).toBeDefined();
    expect(typeof StoryReactionOverlay).toBe('object');
  });
});
